// Standalone constants — no platform dependencies
export const COOKIE_NAME = "operaai_session";
export const ONE_YEAR_MS = 365 * 24 * 60 * 60 * 1000;

// Unused in standalone mode — kept for compatibility
export const getLoginUrl = () => "/";
